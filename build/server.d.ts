import type { ServerBuild } from '@remix-run/cloudflare';

// Type-only stub for local `tsc` runs.
// The real `build/server` module is generated by the Remix build pipeline.
declare const build: ServerBuild;

export = build;

